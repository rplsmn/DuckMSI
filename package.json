{
  "name": "duckdb-wasm-app",
  "version": "1.0.0",
  "description": "Simple WASM DuckDB web application",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.28.0"
  }
}
